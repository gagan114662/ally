[tool:pytest]
addopts = -q --maxfail=1 --disable-warnings
testpaths = tests
pythonpath = .
markers =
    smoke: minimal happy-path checks (always green)
    unit: small, isolated, fast tests
    integration: multi-module, no network
    contract: Phase 0–12 contract tests
    offline: never touches external network
    live: requires ALLY_LIVE=1 (skipped in CI)
    slow: long running (skipped in CI)