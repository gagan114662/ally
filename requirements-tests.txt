# Test execution dependencies
pytest>=8.0.0
pytest-cov>=4.0.0
coverage>=7.0.0

# Core dependencies that tests import
pydantic>=2.0.0
pandas>=2.0.0
numpy>=1.24.0

# Optional dependencies for specific test suites
# (these may fail but shouldn't break the build)
typer>=0.9.0
orjson>=3.8.0
duckdb>=0.9.0

# Web and API testing
requests>=2.28.0
httpx>=0.24.0

# Data science stack (used by various phases)
scipy>=1.10.0
scikit-learn>=1.3.0

# Time series and financial data
yfinance>=0.2.0

# Optional: Graphics and visualization (may not be available in all environments)
# matplotlib>=3.7.0
# plotly>=5.15.0